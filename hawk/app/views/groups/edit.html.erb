<%= render 'form' %>
<script type="text/javascript">
$(function() {
  $("#group_submit").val("<%= escape_javascript(_('Apply Changes')) %>")
  $("#group_revert").show();
  $("#group_id").attr("disabled", "disabled");
  $("#group_children").val([]);
  $("#group_children").attr("disabled", "disabled");
  $("#available").attr("disabled", "disabled");
  $("#group_submit").attr("disabled", "disabled");
  $("#group_revert").attr("disabled", "disabled");
});
</script>

