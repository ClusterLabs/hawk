.panel.panel-default
  .panel-body{ data: { help_target: "#rightbar > .container-fluid > #helpentries > .attrhelp" } }
    = form_for [cib, node], horizontal: true, bootstrap: true do |main_form|
      = errors_for node

      = main_form.text_field :id, label: _("ID"), readonly: true
      = main_form.text_field :name, label: _("Name"), readonly: true

      %fieldset{ data: {attrlist: node.params.to_json, attrlist_mapping: node.mapping.to_json, attrlist_prefix: "node[params]", attrlist_target: '.content', attrlist_freeform: 'yes' }}
        %legend
          = _("Attributes")
          %span.pull-right.toggleable
            = icon_tag("chevron-up")
        .content

      = main_form.button_group do
        = apply_button(main_form, node)
        = revert_button(main_form, node)

        = link_to _("Back"), edit_cib_config_path(cib_id: cib.id), class: "btn btn-default back"
