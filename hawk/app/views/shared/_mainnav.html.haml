%ul.nav.sidebar-nav
  %li
    %ul
  %li
    = _("Manage")

    %ul
      %li{ class: active_menu_with(:states) }
        = link_to _("Status"), cib_state_path(cib_id: current_cib.id)

      - if current_cib.live?
        %li{ class: active_menu_with(:dashboards) }
          = link_to _("Dashboard"), dashboard_path

      - if current_cib.live?
        %li{ class: active_menu_with(:reports) }
          = link_to _("History"), reports_path

  - unless current_cib.offline?
    %li
      = _("Configure")

      %ul
        %li{ class: active_menu_with(:resources, :primitives, :clones, :masters, :templates) }
          = link_to _("Add a resource"), types_cib_resources_path(cib_id: current_cib.id)
        %li{ class: active_menu_with(:constraints, :locations, :colocations, :orders, :tickets) }
          = link_to _("Add a constraint"), types_cib_constraints_path(cib_id: current_cib.id)
        %li{ class: active_menu_with(:wizards) }
          = link_to _("Choose a wizard"), cib_wizards_path(cib_id: current_cib.id)
        %li{ class: active_menu_with(:cib, :graphs) }
          = link_to _("Edit"), edit_cib_path(current_cib.id)
        %li{ class: active_menu_with(:crm_config) }
          = link_to _("Cluster Configuration"), edit_cib_crm_config_path(cib_id: current_cib.id)
        - if current_cib.live?
          %li{ class: active_menu_with(:pages) }
            = link_to _("Command Log"), commands_path

  - if Util.has_feature? :acl_support
    %li
      = _("Access Control")

      %ul
        %li{ class: active_menu_with(:roles) }
          = link_to _("Roles"), cib_roles_path(cib_id: current_cib.id)

        %li{ class: active_menu_with(:users) }
          = link_to _("Targets"), cib_users_path(cib_id: current_cib.id)
