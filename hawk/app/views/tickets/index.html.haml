.container-fluid
  .row
    %h1
      = icon_text "ticket", _("Tickets"), class: "page"
      .pull-right
        .btn-group{ role: "group" }
          = link_to icon_text("plus", _("Create")), new_cib_ticket_path, class: "btn btn-default"

  .row
    .panel.panel-default
      %table.table.table-striped.tickets

- content_for :rightbar do
  .container-fluid{ data: { spy: "affix" } }
    .row.ticket
      = localized_help_for :constraints, :ticket
