<?xml version="1.0"?>
<workflow name="30-mariadb">

<shortdesc lang="en">MariaDB SQL Database</shortdesc>
<longdesc lang="en">Configures a standalone MariaDB database instance.

Note: This wizard does not configure the SQL database itself,
only the cluster resource which manages it.
</longdesc>

<templates>
<template name="mariadb" required="1">
<stepdesc lang="en">
The MariaDB resource.
</stepdesc>
</template>
</templates>

<crm_script>
</crm_script>

</workflow>
