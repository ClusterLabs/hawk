* probably also need to get rid of everything in script/ except rails
* need to test CibObject exceptions (see ApplicationController::initialize)
* need to rework main layout based on new application layout (e.g.: csrf headers, includes etc.)

